app_id: "core-rest-example"
app_version: "1.0.0"

server:
  port: 8080
  cors:
    enabled: true
    allow_origins: ["*"]
    allow_methods: ["GET", "POST", "PUT", "DELETE"]

log:
  level: "info"
  format: "console" # use 'json' for production

worker:
  count: 5
  queue_size: 100
